# v5 Gallery Image Update

Standalone static preview after updating project and beyond images, adding the hobbies gallery, updating the contact coffee section, and softening project detail buttons.

Open `index.html` directly in a browser. This version uses plain HTML, CSS, and JavaScript only.
