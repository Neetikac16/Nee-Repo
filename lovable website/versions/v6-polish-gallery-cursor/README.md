# v6 Polish Gallery Cursor

Standalone static preview after slower rotating galleries, achievement photo rotation, larger coffee image, simpler hobbies captions, terracotta hero CTA, updated selected-work headline, and paw cursor.

Open `index.html` directly in a browser. This version uses plain HTML, CSS, and JavaScript only.
