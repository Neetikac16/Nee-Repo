# Website Versions

This folder stores standalone snapshots of the website preview so each pushed iteration can be opened independently.

- `v4-alignment-cleanup/` - static HTML/CSS/JS preview after the PMO/AI positioning and alignment cleanup.

The active working preview remains in `../preview/`.
