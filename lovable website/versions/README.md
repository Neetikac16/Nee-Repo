# Website Versions

This folder stores standalone snapshots of the website preview so each pushed iteration can be opened independently.

- `v4-alignment-cleanup/` - static HTML/CSS/JS preview after the PMO/AI positioning and alignment cleanup.
- `v5-gallery-image-update/` - static HTML/CSS/JS preview after image swaps, hobbies gallery, coffee contact update, and project toggle refinements.
- `v6-polish-gallery-cursor/` - static HTML/CSS/JS preview after slower galleries, achievement rotation, larger coffee, simpler hobby captions, terracotta CTA, updated project headline, and paw cursor.

The active working preview remains in `../preview/`.
